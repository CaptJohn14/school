#------------------------------------------------------------------------------
# Name: Johnson Le
# cruzID: jonle
# id number: 1512346
# File Name: Makefile3
# Description: Make for FileTokens
#------------------------------------------------------------------------------

JAVASRC    = FileTokens.java
SOURCES    = README Makefile $(JAVASRC)
MAINCLASS  = FileTokens
CLASSES    = FileTokens.class
JARFILE    = FileTokens.jar

all: $(JARFILE)

$(JARFILE): $(CLASSES)
	echo Main-class: $(MAINCLASS) > Manifest
	jar cvfm $(JARFILE) Manifest $(CLASSES)
	rm Manifest

$(CLASSES): $(JAVASRC)
	javac -Xlint $(JAVASRC)

clean:
	rm $(CLASSES) $(JARFILE)
